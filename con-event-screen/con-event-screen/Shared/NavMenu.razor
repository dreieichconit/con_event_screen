<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">con-event-screen</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="marquee">
                <span class="oi oi-plus" aria-hidden="true"></span> Edit Marquee
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="notification">
                <span class="oi oi-warning" aria-hidden="true"></span> Fire Alert
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="event-screen">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Screen
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

}